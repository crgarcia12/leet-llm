# Liliput serves this static preview behind a path-stripping reverse proxy.
FROM mcr.microsoft.com/azurelinux/base/python:3.12
WORKDIR /app
COPY preview/ ./
EXPOSE 80
CMD ["python3", "-m", "http.server", "80", "--bind", "0.0.0.0", "--directory", "/app"]
