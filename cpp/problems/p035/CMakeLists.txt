add_executable(p035_oracle cpu_oracle.cpp)
target_link_libraries(p035_oracle PRIVATE leetllm_core)
if(BUILD_TESTING)
  add_test(NAME p035_oracle_test COMMAND p035_oracle)
endif()

if(LEETLLM_ENABLE_CUDA)
  add_executable(p035_cuda_starter cuda_starter.cu)
  add_executable(p035_cuda_solution cuda_solution.cu)
  foreach(target p035_cuda_starter p035_cuda_solution)
    target_include_directories(${target} PRIVATE ${PROJECT_SOURCE_DIR}/cpp/cuda)
    target_link_libraries(${target} PRIVATE CUDA::cudart)
    set_target_properties(${target} PROPERTIES CUDA_ARCHITECTURES "${CMAKE_CUDA_ARCHITECTURES}")
  endforeach()
  if(BUILD_TESTING)
    add_test(NAME p035_cuda_solution_test COMMAND p035_cuda_solution)
    set_tests_properties(p035_cuda_solution_test PROPERTIES LABELS "cuda")
  endif()
endif()
