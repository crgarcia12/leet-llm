add_executable(p028_oracle cpu_oracle.cpp)
target_link_libraries(p028_oracle PRIVATE leetllm_core)
if(BUILD_TESTING)
  add_test(NAME p028_oracle_test COMMAND p028_oracle)
endif()

if(LEETLLM_ENABLE_CUDA)
  add_executable(p028_cuda_starter cuda_starter.cu)
  add_executable(p028_cuda_solution cuda_solution.cu)
  foreach(target p028_cuda_starter p028_cuda_solution)
    target_include_directories(${target} PRIVATE ${PROJECT_SOURCE_DIR}/cpp/cuda)
    target_link_libraries(${target} PRIVATE CUDA::cudart)
    set_target_properties(${target} PROPERTIES CUDA_ARCHITECTURES "${CMAKE_CUDA_ARCHITECTURES}")
  endforeach()
  if(BUILD_TESTING)
    add_test(NAME p028_cuda_solution_test COMMAND p028_cuda_solution)
    set_tests_properties(p028_cuda_solution_test PROPERTIES LABELS "cuda")
  endif()
endif()
